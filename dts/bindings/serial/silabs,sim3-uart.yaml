---
title: SIM3 UART
version: 0.1

description: >
    This binding gives a base representation of the SIM3 UART

inherits:
    !include uart.yaml

properties:
    compatible:
      constraint: "silabs,sim3-uart"

    reg:
      type: array
      description: mmio register space
      generation: define
      category: required

    interrupts:
      type: compound
      category: required
      description: required interrupts
      generation: define

...
